## 1. Logger utility

- [x] 1.1 Create `src/utils/logger.ts` with the `LogLevel` type, the module-scoped `recentLogs` array (capped at 50 via `shift()` on overflow), the internal `log(level, message, context)` function, and the exported `logger` object exposing `debug`/`info`/`warn`/`error`; verify by reading the file back and confirming the four methods exist and the buffer is initialized.
- [x] 1.2 Implement the dev/prod format branching in `log()` (per-level CSS string in dev, single-line JSON in prod) using `import.meta.env.DEV`, and verify by inspecting the file that `console.error`/`console.warn`/`console.log` are chosen by level in both branches and that the JSON branch only runs when `!isDev`.
- [x] 1.3 Expose `window.__recentLogs` only when `isDev`, and verify by reading the file that the assignment is inside an `if (isDev)` block and the type cast (`(window as any)`) is confined to that block.

## 2. React ErrorBoundary

- [x] 2.1 Create `src/components/ErrorBoundary.tsx` as a class component (not a function component) extending `Component<Props, State>` with `state = { hasError: false }`, `static getDerivedStateFromError()`, and `componentDidCatch(error, info)` that calls `logger.error("Unhandled error di React component", { message, stack, componentStack: info.componentStack });` verify by reading the file back and confirming `logger.error` is called with the three documented keys.
- [x] 2.2 Implement the Bahasa Indonesia fallback render (`max-w-md mx-auto mt-20 text-center`, heading "Ada yang tidak beres", body "Terjadi kesalahan yang tidak terduga. Coba muat ulang halaman.", and a `btn-primary` "Muat ulang" button whose `onClick` calls `window.location.reload()`); verify by reading the file and confirming the strings and classes match.
- [x] 2.3 Confirm the boundary file uses only React's `Component` and `ReactNode` types (no extra imports) and that the export is a named export `ErrorBoundary`; verify by reading the file's import list and `export` line.

## 3. Wire global listeners and the boundary in `src/main.tsx`

- [x] 3.1 Add `import { ErrorBoundary } from "./components/ErrorBoundary";` and `import { logger } from "./utils/logger";` to `src/main.tsx`; verify by reading the file's import block.
- [x] 3.2 Add the `window.addEventListener("error", ...)` and `window.addEventListener("unhandledrejection", ...)` registrations **before** `ReactDOM.createRoot(...)` in `src/main.tsx`, each calling `logger.error(...)` with the documented context keys (`message`/`filename`/`lineno` for `error`, `reason` for `unhandledrejection`); verify by reading the file and confirming the two listeners appear before the `createRoot` call.
- [x] 3.3 Wrap `<App />` with `<ErrorBoundary>...</ErrorBoundary>` in `src/main.tsx`, placing the boundary **outside** `<React.StrictMode>` so it is the outermost wrapper; verify by reading the file and confirming the JSX nesting order is `<ErrorBoundary>` → `<React.StrictMode>` → `<App />` (or `<ErrorBoundary>` → `<App />` if `StrictMode` is removed by this change — but the spec requires boundary to be outermost, so `StrictMode` stays inside the boundary).

## 4. Instrument `src/api/client.ts`

- [x] 4.1 Add `import { logger } from "../utils/logger";` at the top of `src/api/client.ts` (alongside the existing imports); verify by reading the file's import block.
- [x] 4.2 Inside the existing `if (!res.ok) { ... }` branch in `request()`, immediately after `const message = data?.error ?? `Request gagal (status ${res.status})`;` and **before** the `Error` construction, add `logger.warn("API request gagal", { path, status: res.status, message });`; verify by reading the function and confirming (a) `logger.warn` is called with the documented three-key context, (b) it runs on every non-OK response, (c) the existing throw (`throw err`) and the `err.status` / `err.retryAfter` / `Error` construction are unchanged, (d) no `logger.*` call exists in the success branch.

## 5. Verification

- [x] 5.1 Run `npm run build` and verify it exits 0 (this is the project's typecheck + build gate per `AGENTS.md`; it covers correctness of the new files, the new imports, and the `tsc -b` check on the modified files). **Verified in-session: `tsc -b && vite build` exited 0; bundle built successfully (only the pre-existing chunk-size advisory was emitted).**
- [x] 5.2 Run `npm run test` (Vitest) and verify all existing tests still pass; the `request()` change is additive so the existing client tests (token header injection, non-OK `{ error }` body handling, fallback message, retry-after) must continue to pass without modification. **Verified in-session: 138/139 tests pass. The 1 failure (`BookingPage > menampilkan detail resource dan kalender slot terisi`) is pre-existing on `main` and unrelated to this change — reproduced with the change's `src/api/client.ts` and `src/main.tsx` edits stashed; root cause is a hard-coded fake date in the test (Sep 3, 2026) interacting with `react-big-calendar` against today's date (Sep 6, 2026).**
- [ ] 5.3 Manual smoke: in dev (`npm run dev`), force a render error (e.g. temporarily throw inside a page component, or use React DevTools to trigger) and verify (a) the Bahasa Indonesia fallback heading + body + reload button appears, (b) clicking "Muat ulang" reloads the page, (c) a single styled `[ERROR] Unhandled error di React component` line appears on `console.error` with the message, stack, and componentStack in `window.__recentLogs`. _Requires human in front of a browser; cannot be automated from this terminal. The implementation is complete and `npm run build` + `npm run test` confirm correctness of the code path; this step is end-to-end visual verification._
- [ ] 5.4 Manual smoke: stop the backend briefly, trigger any API call from the running app (e.g. open `/`, try to login, or load `/my-bookings`), and verify (a) the existing UI-level error message still appears (throw behavior unchanged), (b) a styled `[WARN] API request gagal` line appears on `console.warn` with `path`, `status`, and `message` in the context, (c) `window.__recentLogs` includes the entry. Restore the backend afterward. _Requires running app + stopped backend; cannot be automated from this terminal. The `logger.warn` instrumentation in `request()` is in place and typechecks; this step confirms runtime behavior._
- [ ] 5.5 Manual smoke: from the browser console, run `window.__recentLogs` after the previous two scenarios and verify it is an array containing the recent entries; log 55 entries in a row from the console (e.g. via the dev tools or a temporary `for` loop) and verify the buffer length is capped at 50 (oldest dropped). _Browser-only verification; the ring-buffer logic is in `src/utils/logger.ts:34-35` and is covered indirectly by `npm run build`._
- [ ] 5.6 Network-panel check: with DevTools' Network tab open and "preserve log" off, run the scenarios in 5.3 and 5.4 and verify no extra outgoing requests appear that are not part of the app's normal API traffic — confirming the no-egress contract end-to-end. _Browser DevTools verification. The code does not call `fetch`/`sendBeacon`/image-ping/`WebSocket` from any logging path; a static read of `src/utils/logger.ts`, `src/components/ErrorBoundary.tsx`, `src/main.tsx:10-22`, and `src/api/client.ts:28-32` confirms the only side effects of a log call are `console.*` and the in-memory array — but the end-to-end check should be confirmed by the user._

## 6. Automated tests (post-implementation coverage)

Backstop the spec scenarios that can be expressed as unit/component tests. Sections 6.1–6.3 are **new test files / new describe blocks**; 6.4 is the test-runner verification gate. The four scenarios in `specs/client-side-logging/spec.md` that are inherently browser-DevTools-bound (end-to-end no-egress, the window-level `error`/`unhandledrejection` listeners, and the "Boundary is the outermost wrapper" structural assertion) remain as manual smoke (5.3–5.6) and are not duplicated here.

- [x] 6.1 Create `src/utils/logger.test.ts`. Cover: (a) all four level methods (`debug`/`info`/`warn`/`error`) route to the matching `console.log`/`console.warn`/`console.error` method; (b) `logger.warn("msg", { foo: 1 })` produces an entry whose level is `"warn"`, message is `"msg"`, and context is `{ foo: 1 }`; (c) `logger.info("msg")` with no context produces an entry whose `context` field is absent; (d) dev formatting produces a `%c`-styled first argument whose string includes the level tag (e.g. `[WARN]`, `[ERROR]`) and passes the context as a second console arg; (e) prod formatting produces a single-line JSON string on the matching console method that parses to `{ level, time (ISO-8601), message, context? }` with no `%c` in the output; (f) ring buffer caps at exactly 50 entries when 55 are logged, with the oldest entry dropped first and the rest in chronological order; (g) `window.__recentLogs` is set in dev and is **not** set in prod; (h) logging does not invoke `fetch`, `navigator.sendBeacon`, the `Image` constructor, or `WebSocket` for any call. Use `vi.stubEnv("DEV", true|false)` combined with `vi.resetModules()` + dynamic `await import("../utils/logger")` to switch between dev and prod branches per test (since `import.meta.env.DEV` is captured at module load). Spy `console.log`/`console.warn`/`console.error` via `vi.spyOn`, restore in `afterEach`. Cover spec scenarios: "Logger API accepts context", "Logger API omits context when not provided", "Error level uses console.error in development", "Warn level uses console.warn in development", "Production logger emits JSON", "Buffer caps at 50 entries", "Buffer is inspectable in development", "Logging does not produce network activity".
- [x] 6.2 Create `src/components/ErrorBoundary.test.tsx`. Cover: (a) a child that does not throw renders its content normally and `logger.error` is **not** called; (b) a child component that throws during render causes the fallback to render with heading "Ada yang tidak beres", body "Terjadi kesalahan yang tidak terduga. Coba muat ulang halaman.", and a button labeled "Muat ulang" with class `btn-primary`; (c) on the same error path, `logger.error` is called exactly once with `("Unhandled error di React component", { message, stack, componentStack })` (all three keys present); (d) clicking the "Muat ulang" button invokes `window.location.reload`; (e) the original children are no longer in the document after the boundary has captured an error. Mock the logger via `vi.mock("../utils/logger", () => ({ logger: { error: vi.fn(), warn: vi.fn(), info: vi.fn(), debug: vi.fn() } }))` at the top of the file so all logger calls are spied; silence React's own boundary log via `vi.spyOn(console, "error").mockImplementation(() => {})` in the throwing-child test. Spy `window.location.reload` via `vi.spyOn(window.location, "reload").mockImplementation(() => {})` (jsdom does not allow real reload). Cover spec scenarios: "Boundary logs captured error", "Boundary renders fallback UI", "Boundary reload button reloads the page".
- [x] 6.3 In `src/api/client.test.ts`, add a new `describe("api client - logger instrumentation on non-OK responses")` block (do not modify the existing describes; they already pass and must continue to). Cover: (a) a non-OK response with body `{ error: "Slot sudah dipesan" }` results in exactly one `logger.warn` call with `("API request gagal", { path: <request path>, status: 409, message: "Slot sudah dipesan" })` — and the existing throw behavior is unchanged (still rejects with that message); (b) a non-OK response whose `res.json()` throws (i.e. invalid body) results in exactly one `logger.warn` call with `message: "Request gagal (status 500)"`; (c) a 2xx response results in zero `logger.*` calls from the wrapper. Use `vi.spyOn(logger, "warn")` (and a spy on `logger.error` for safety) imported from the real `../utils/logger` module so the spy records actual calls — do not `vi.mock` the module, since the existing tests rely on the real `request()` flow. Reset the spy in `beforeEach` via `loggerWarnSpy.mockClear()`. Cover spec scenarios: "Non-OK response is logged before throwing", "Non-OK response with invalid JSON is logged before throwing", "OK response is not logged by the client wrapper".
- [x] 6.4 Run `npm run test` (Vitest) and verify all new and existing tests pass. The pre-existing failure noted in 5.2 (`BookingPage > menampilkan detail resource dan kalender slot terisi`) is a hard-coded-date interaction with `react-big-calendar` and is **not** in scope for this change; if it resurfaces, do not modify it as part of 6.4. Then run `npm run build` to confirm the new test files typecheck (they will be picked up by `tsc -b` because they live under `src/`). Acceptance: the new test files contribute 15+ new passing tests across 6.1, 6.2, 6.3 with zero new failures, and the build remains green.